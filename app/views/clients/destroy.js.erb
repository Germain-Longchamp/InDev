$('.card-client__delete').bind('ajax:success', function() {
	$('.popin').removeClass('displayed');
	let dataName = $(this).parents('.col-card-client').attr('data-name')
	$('.col-card-client[data-name="' + dataName + '"]').fadeOut();
});